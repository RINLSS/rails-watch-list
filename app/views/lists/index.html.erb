<h1>Lists</h1>

<% @lists.each do |list| %>
<% end %>

<ul class="list-group">
  <% @lists.each do |list| %>
    <li class="list-group-item"><%= link_to list.name, list %></li>
  <% end %>
</ul>

<%= link_to "New list", new_list_path %>
